import { Event } from "../../types/Event";
export interface EventCardProps {
    event: Event;
}
export declare const EventCard: ({ event }: EventCardProps) => import("react/jsx-runtime").JSX.Element;
